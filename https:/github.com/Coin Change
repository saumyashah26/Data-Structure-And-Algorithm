long long int count( int s[], int m, int n )
    {
        //code here.
        vector<long long int> ans(n+1,0);
        ans[0]=1;
        for(int i=0;i<m;i++){
            for(int j=s[i];j<=n;j++){
                ans[j]+=ans[j-s[i]];
            }
        }
        return ans[n];
    }
